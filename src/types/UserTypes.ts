export interface User {
    id: number,
    avatar_url: null,
    bio: null,
    created_at: string
    email: string,
    first_name: string,
    last_name: string,
    password: string,
    user_role: string
}